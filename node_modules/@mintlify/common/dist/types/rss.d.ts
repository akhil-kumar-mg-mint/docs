export type RSSItem = {
    title: string;
    date: string;
    description?: string;
    categories?: string[];
};
export type SavedRssFile = {
    rssPath: string;
    updates: RSSItem[];
    orgName: string;
    orgDescription?: string;
    title?: string;
    description?: string;
};
